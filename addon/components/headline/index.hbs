{{#let (element (bs-default @as "h1")) as |Element|}}
  <Element class="headline my-4 py-4" ...attributes>
    {{@title}}
    {{#if @subtitle}}
      <small class="d-block text-muted mt-1 font-weight-light">{{@subtitle}}</small>
    {{/if}}
  </Element>
{{/let}}